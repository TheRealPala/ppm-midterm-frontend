import React, { FC } from 'react';
import './gridComposition.scss';

interface gridCompositionProps {}

const gridComposition: FC<gridCompositionProps> = () => (
  <div className="gridComposition">
    gridComposition Component
  </div>
);

export default gridComposition;
