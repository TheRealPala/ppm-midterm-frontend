import React, { FC } from 'react';
import './navCanvas.scss';

interface navCanvasProps {}

const navCanvas: FC<navCanvasProps> = () => (
  <div className="navCanvas">
    navCanvas Component
  </div>
);

export default navCanvas;
